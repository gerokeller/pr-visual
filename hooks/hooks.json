{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "if echo \"$TOOL_INPUT\" | grep -q 'gh pr create'; then echo '>> Run /pr-visual to generate visual documentation for this PR'; fi"
          }
        ]
      }
    ]
  }
}
